name: learn-github-actions
on: [push]
jobs:
  testbuild:
    runs-on: ubuntu-latest
    steps:
      - name: Install Angular CLI
        run: npm install -g @angular/cli
      - name: Test Angular CLI
        run: ng help
