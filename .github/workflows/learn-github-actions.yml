name: learn-github-actions
on: [push]
jobs:
  testbuild:
    runs-on: ubuntu-latest
    steps:
      - name: Install Power Apps CLI
        shell: pwsh
        run: Install-Package Microsoft.PowerApps.CLI
